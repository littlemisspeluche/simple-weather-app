import styled from 'styled-components';

export const AppWrapper = styled.div`
  position: relative;
  margin: 0;

  p, input {
    font-family: PT Sans;
  }

  p {
    color: #F0F0F0;
    opacity: 0.8;
  }

  .text-dark {
    color: #000000;
    opacity: 1;
  }

hr {
  border: 1px solid rgba(56, 56, 116, 0.15);
  margin: 12px 0;
}
`